---
version: '3'

networks:
  redis:
    name: redis

services:

  app:
    image: node18:redis
    networks:
      - redis

  redis:
    image: redis:alpine3.17
    networks:
      redis:
        aliases:
          - redis
